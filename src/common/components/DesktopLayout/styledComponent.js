import styled from '@emotion/styled';
import tw from 'tailwind.macro';

const UserObservationsListContainer = styled.div `${tw `flex flex-col justify-center items-center`}`;

export { UserObservationsListContainer };
